# Example env file for local development
# Use a server-only TMDB API key (do NOT prefix with NEXT_PUBLIC_ for production)
TMDB_API_KEY=your_tmdb_api_key_here

# If you need to expose for local debugging, you can also set (not recommended):
# NEXT_PUBLIC_THE_MOVIE_DB_API_KEY=your_tmdb_api_key_here
