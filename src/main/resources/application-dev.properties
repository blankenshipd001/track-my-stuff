## MongoDB properties

# Setting the mongodb.port to 0 tells the framework to start the embedded db on a random port which allows the tests to run in parallel
spring.data.mongodb.port=0

#mongo.db.name=movie_testDB

##external Mongo url
#spring.data.mongodb.uri=mongodb://localhost:27017/movieDB
#mongo.db.url=localhost